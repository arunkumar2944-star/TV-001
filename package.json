{
  "name": "trichy-vision",
  "version": "1.0.0",
  "private": true,
  "description": "Trichy Vision - internal news publishing management system",
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "test": "npm --prefix backend test",
    "lint": "npm --prefix backend run lint && npm --prefix frontend run lint",
    "verify:schema": "npm --prefix backend run verify:schema",
    "seed:platforms": "npm --prefix backend run seed:platforms",
    "seed:users": "npm --prefix backend run seed:users"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
